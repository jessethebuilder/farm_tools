<%= form_for(@test_company) do |f| %>
  <% if @test_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_company.errors.count, "error") %> prohibited this test_company from being saved:</h2>

      <ul>
      <% @test_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
