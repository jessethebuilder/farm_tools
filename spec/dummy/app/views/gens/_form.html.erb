<%= form_for(@gen) do |f| %>
  <% if @gen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gen.errors.count, "error") %> prohibited this gen from being saved:</h2>

      <ul>
      <% @gen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
